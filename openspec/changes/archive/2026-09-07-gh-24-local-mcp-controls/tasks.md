## 1. Shared dependency and command service

- [x] 1.1 Pin the delivered hub MCP tarball and hash/license receipt; verify a clean consumer install and built-server import using Node 24.
- [x] 1.2 Extract canonical display admission with retained snapshot, timing and effect outcomes; observe focused failures then pass HTTP-first/MCP-first replay, conflict and failed-result tests while preserving HTTP shapes.
- [x] 1.3 Verify the existing epoch, request-order and 256-completed retention behavior through focused service tests, including no second execution after a lost response.

## 2. Player evidence

- [x] 2.1 Add requested, acknowledged and probe-observed display evidence with monotonic source times; pass injected-clock tests for absent telemetry, repeated reads and restart with no device writes.
- [x] 2.2 Update current background upload/reconnect and direct control observations; pass stale-generation and possible-effects tests without changing existing pause/recovery semantics.

## 3. Optional authenticated transport

- [x] 3.1 Add explicit MCP configuration and private bounded credential provisioning/revocation; pass default-disabled, malformed/oversize file, current-session revocation and sanitized-error tests on supported hosts.
- [x] 3.2 Bind the three strict shared extensions and safe status/outcome projections; pass schema, scope, no-private-data and invalid-input-before-admission tests.
- [x] 3.3 Mount raw MCP handling before Fastify parsing with exact-route browser-header exception and actual-port authority checks; pass built-server SDK initialization/call tests and existing API security tests.
- [x] 3.4 Integrate raw-response accounting and preClose cleanup; pass real-socket completion/cancellation/disconnect/overload/shutdown tests showing the backend survives client departure and releases capacity.

## 4. Integration and delivery evidence

- [x] 4.1 Verify concurrent HTTP/MCP commands use one adapter and preserve upload FIFO, screen-off pause, screen-on non-resume, retained failures and timeout uncertainty through fake-transport integration tests.
- [x] 4.2 Document the dependency receipt, ADR, local credential commands, Codex template, Windows/WSL diagnostics and rollback; verify example inputs with synthetic credentials without modifying personal settings or operating hardware.
- [x] 4.3 Run Node 24 application, workflow and browser checks on the applicable Linux/Windows matrix; retain results as source evidence under the approved local-validation exception.
- [x] 4.4 Obtain successful current OpenSpec validation and archive instructions, verify every affected requirement, synchronize all three capability deltas and archive the completed change before independent fixed-head review.
