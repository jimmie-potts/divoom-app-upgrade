## 1. Contract and protected reads

- [x] 1.1 Pin the released artifact and verify checksum, shared fixtures and schema imports.
- [x] 1.2 Add opt-in configured native snapshot access; prove credential scope, strict identity, browser protections and simulator unknown telemetry with owning HTTP tests.

## 2. Commands and feeds

- [x] 2.1 Integrate native commands with the existing ledger and service methods; prove duplicate/conflict/expiry, stale revisions/generations, shared browser/MCP ownership, screen behavior and uncertain outcomes.
- [x] 2.2 Add bounded native snapshot events; prove replay/resync, revoked streams, slow-client handling and shutdown without command effects.
- [x] 2.3 Prove guarded media capture, catalog changes and bounded supported capabilities using existing library/player services.

## 3. Delivery validation

- [x] 3.1 Document configuration, compatibility, recovery and evidence limits, with an ADR and linked hub guide synchronization.
- [x] 3.2 Run npm run check and npm run test:browser using Node 24; inspect the actual spec inventory and archive the complete change after successful current CLI lookups.
