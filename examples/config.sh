# Optional overrides. Export in your shell before npm run simulator.
# Use an absolute data path outside any source checkout. No device IP is needed.
export PIXOO_DATA_DIR="$HOME/.local/share/pixoo-playlist-controller"
export PIXOO_PORT=8787
export PIXOO_MODE=simulator
